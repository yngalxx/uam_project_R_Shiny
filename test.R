library(dplyr)

usa_pop <- read.csv("data/China/china_population.csv")
china_pop <- read.csv("data/USA/population_usa.csv")

china_pop

# mark code and then go to "Code > Reformat code" to use code auto-format in R 